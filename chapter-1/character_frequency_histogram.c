#include <ctype.h>
#include <stdio.h>

// Prints histogram of character frequencies

const int in = 1;
const int out = 0;
const int num_chars = 26;
const int h_length = 10 + 2 * num_chars;

int main() {
  int i = 0, state = 0;
  int frequencies[num_chars] = {0};

  state = out;
}
